<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "S Coin" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <%= favicon_link_tag 'S_coin.png', type: 'image/png' %>
<%= favicon_link_tag 'S_coin.svg', type: 'image/svg+xml' %>

<!-- Apple Touch Icon (for iOS devices) -->
<link rel="apple-touch-icon" href="<%= asset_path('S_coin.png') %>">


    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-r from-indigo-700 via-purple-700 to-pink-700 px-4">
    <main class="container  ">
    <%= render "shared/Nav" %>
    <%= render "shared/toster" %>
      <%= yield %>
    </main>
    <%= render "shared/footer" %>
  </body>
</html>
